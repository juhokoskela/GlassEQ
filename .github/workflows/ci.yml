name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  swift:
    name: Swift build and tests
    runs-on: xcode-27
    env:
      DEVELOPER_DIR: /Applications/Xcode_27.0.app/Contents/Developer

    steps:
      - name: Check out
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Show toolchain
        run: |
          xcodebuild -version
          swift --version

      - name: Build
        run: swift build

      - name: Check release channels
        run: python3 Scripts/test-release-app.py

      - name: Test
        run: swift test

      - name: Measure release DSP performance
        run: swift run -c release GlassEQDiagnostics dsp-benchmark

      - name: Build release app bundle
        run: ./Scripts/build-release-app.sh

      - name: Verify signatures
        run: |
          codesign --verify --strict --verbose=2 .build/release-app/GlassEQ.app/Contents/Helpers/GlassEQSettings.app
          codesign --verify --strict --verbose=2 .build/release-app/GlassEQ.app
